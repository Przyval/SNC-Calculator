{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "mkdir -p database storage/framework/cache/data storage/framework/sessions storage/logs && touch database/database.sqlite && php artisan migrate:fresh --seed --force && php artisan storage:link || true && php -S 0.0.0.0:$PORT -t public",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}